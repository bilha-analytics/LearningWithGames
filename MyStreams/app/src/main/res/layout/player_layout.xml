<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="111dp"
        android:layout_gravity="center|center_horizontal|center_vertical">

        <ImageButton
            android:id="@+id/btn_prev"
            android:layout_width="123dp"
            android:layout_height="94dp"
            android:adjustViewBounds="true"
            android:background="#00FFFFFF"
            android:cropToPadding="true"
            android:onClick="btnPreviousClicked"
            android:scaleType="fitCenter"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/play_prev" />

        <ImageButton
            android:id="@+id/btn_random"
            android:layout_width="122dp"
            android:layout_height="94dp"
            android:layout_marginStart="20dp"
            android:layout_marginLeft="20dp"
            android:background="#00FFFFFF"
            android:onClick="btnRandomClicked"
            android:scaleType="fitCenter"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/btn_prev"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.47"
            app:srcCompat="@drawable/play_random" />

        <ImageButton
            android:id="@+id/btn_next"
            android:layout_width="122dp"
            android:layout_height="94dp"
            android:layout_marginStart="20dp"
            android:layout_marginLeft="20dp"
            android:background="#00FFFFFF"
            android:onClick="btnNextClicked"
            android:scaleType="fitCenter"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/btn_random"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.47"
            app:srcCompat="@drawable/play_next" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</LinearLayout>